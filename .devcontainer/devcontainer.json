{
  "name": "",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "overrideCommand": false,
  "initializeCommand": {
    "set-project-name": "bash -c 'source .devcontainer/.env && PROJECT_NAME=${PROJECT_NAME:-myproject}; sed -i \"s//$PROJECT_NAME/g\" ${localWorkspaceFolder}/.devcontainer/devcontainer.json'",
    "ensure-adb-infrastructure": "bash -c 'if docker ps --format \"table {{.Names}}\" | grep -q \"^shared-adb-server$\"; then echo \"✅ ADB server already running\"; else echo \"🔧 ADB server not found - will attempt to start infrastructure...\"; docker compose -p infrastructure -f ../../../infrastructure/mobile/android/adb/docker-compose.yml up -d 2>/dev/null || echo \"⚠️ No ADB infrastructure available - manual setup may be required\"; fi'"
  },
  "onCreateCommand": {
    "setup-flutter-project": "echo 'Container created - Flutter setup will run on start'"
  },
  "postStartCommand": [
    "sh -c \"echo '🚀 Container started - initializing Flutter environment...'\"",
    "sh -c \"echo '📱 Checking ADB connection...' && timeout 10 adb devices > /dev/null 2>&1 && echo '✅ ADB service connected' || echo '🔄 ADB service connecting...'\"",
    "sh -c \"flutter --version && echo '✅ Flutter SDK ready' || echo '⚠️  Flutter SDK not ready yet'\"",
    "sh -c \"timeout 300 .devcontainer/scripts/setup-flutter-project.sh && echo '✅ Flutter project setup complete' || echo '🔄 Flutter setup running in background...'\"",
    "sh -c \"echo '🎯 Ready for development!'\""
  ],
  "postAttachCommand": ".devcontainer/scripts/ready-check.sh",
  "containerEnv": {
    "SHELL": "/bin/zsh"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "dart.flutterSdkPath": "/flutter",
        "dart.debugExternalLibraries": true,
        "dart.debugSdkLibraries": false,
        "editor.formatOnSave": true,
        "editor.codeActionsOnSave": {
          "source.fixAll": "explicit"
        },
        "terminal.integrated.shell.linux": "/bin/zsh",
        "terminal.integrated.defaultProfile.linux": "zsh",
        "terminal.integrated.automationProfile.linux": {
          "path": "/bin/zsh",
          "args": [
            "-l"
          ]
        },
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "/bin/zsh"
          },
          "zsh": {
            "path": "/bin/zsh"
          }
        }
      }
    }
  }
}